# Node
node_modules/

# Per-run PDF working directories authored + rendered by the docsmith:builder skill
.docsmith/

# Generated output / previews
*.pdf
*.png

# Transient skill-eval run results (fixtures + evals.json are tracked; run outputs are not)
skills/*-workspace/
