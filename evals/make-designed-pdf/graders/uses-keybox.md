---
type: tool_used
tool: Write
input_match: 'KeyBox'
---
Claude authored the admonition using the real `KeyBox` component. Scoped to Write input, so a
reference doc that names KeyBox cannot satisfy this without the component actually being written.
