---
type: file_exists
path: "**/pdf.tsx"
exists: true
---
Claude scaffolded and authored a `pdf.tsx` document module (the skill writes it under
`.docsmith/<run>/`). Counts only files Claude created during the run.
